1. ќписать своими словами дл€ чего вообще нужны функции в программировании.

‘ункци€ содержит определенную последовательность команд (прописываетс€ в теле функции), котора€ может вызыватьс€ 
неограниченное количество раз в том месте, где необходимо выполнить эти команды, без дублировани€ полного кода.
„астоиспользуемые способы объ€влени€ функции:
    - function declaration - можно вызывать в любом месте кода, даже до ее объ€влени€.
        function им€‘ункции(параметры функции) {тело функции}
    - function expression - не имеет имени, нельз€ вызывать до ее объ€влени€, т.к. она объ€вл€етс€ через переменную.
        const им€ѕеременной = function(параметры функции) {тело функции}.

‘ункци€ может принимать значени€ - аргументы, и возвращать результат.

2. ќписать своими словами, зачем в функцию передавать аргумент.

ѕри вызове функции пользователь может передать ей данные, например, с помощью prompt. ‘ункци€ обрабатывает эти значени€, 
они же подставл€ютс€ в параметры функции согласно заданной закономерности и €вл€ютс€ аргументами функции, после чего 
функци€ возвращает результат(значение). 
„тобы вернуть значение используют return, эти данные могут использоватьс€ в дальнейшей работе программы.
‘ункци€ может содержать неопределенное количество параметров, значение которых может передаватьс€ различными способами.